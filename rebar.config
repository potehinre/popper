%% Здесь будут лежать зависимости
{deps_dir, ["deps"]}.

%% Поддиректории, в которые rebar должен заглядывать
{sub_dirs, ["rel", "apps/popper"]}.

%% Опции компилятора
{erl_opts, [debug_info, fail_on_warning]}.

